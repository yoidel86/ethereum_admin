<%= form_with(model: contrato, local: true) do |form| %>
  <% if contrato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contrato.errors.count, "error") %> prohibited this contrato from being saved:</h2>

      <ul>
      <% contrato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :contrato_name %>
  </div>

  <div class="field">
    <%= form.label :abi %>
    <%= form.text_field :abi, id: :contrato_abi %>
  </div>

  <div class="field">
    <%= form.label :methods %>
    <%= form.text_field :methods, id: :contrato_methods %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :contrato_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
